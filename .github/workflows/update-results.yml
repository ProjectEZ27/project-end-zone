name: Mise à jour des résultats NFL

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  update-results:
    runs-on: ubuntu-latest
    steps:
      - name: Appeler la route de mise à jour
        run: |
          curl -f -X GET "https://projectendzone.fr/api/cron/update-results" \
            -H "AuthorizatioCapture d’écran du 2026-09-10 12-42-52